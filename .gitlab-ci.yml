image: node:21

lint:
  script:
    - npm install
    - npm test

pages:
  script:
    - npm ci
    - npm run build
    - mv dist/ public
  after_script:
  - npm run deploy
  artifacts:
    paths:
    - public
  only:
  - tags
