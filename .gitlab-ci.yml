image: node:10

cache:
  paths:
  - node_modules/

lint:
  script:
    - npm prune
    - npm install
    - npm run build

pages:
  script:
    - npm install
    - npm run build
    - mv dist public
  artifacts:
    paths:
    - public
  only:
  - master
