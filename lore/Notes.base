views:
  - type: table
    name: Table
    filters:
      and:
        - file.folder == "🗡 Dismas Remade"
        - file.ext == "md"
        - "!preview.isEmpty()"
    order:
      - file.name
      - common
      - preview
    sort:
      - property: file.name
        direction: ASC
    columnSize:
      file.name: 428
