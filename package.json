{
  "name": "wyrgud",
  "version": "0.1.0",
  "dependencies": {
    "skeleton-css": "^2.0.4",
    "vellum-dice": "^0.3.0",
    "vellum-doc": "^0.10.1",
    "vellum-random-table": "^1.2.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/plugin-terser": "^0.4.4",
    "browser-sync": "^3.0.4",
    "htmlhint": "^1.7.1",
    "linkinator": "^7.3.0",
    "npm-watch": "^0.13.0",
    "rollup": "^4.52.5",
    "rollup-plugin-copy": "^3.5.0",
    "stylelint": "^16.25.0",
    "stylelint-config-standard": "^39.0.1"
  },
  "scripts": {
    "build": "mkdir -p dist && npm run build:dist && npm run build:components",
    "build:components": "rollup -c",
    "build:dist": "mkdir -p dist && cp -r main.css assets dist/",
    "build:watch": "npm-watch",
    "clean": "rm -r dist",
    "test": "htmlhint 'index.html' && stylelint main.css assets/styles/**/*.css && npx linkinator index.html -ro --recurse --skip '(.*\\.reddit\\.com)|(.*\\.twitter\\.com)|(.*\\.drivethrurpg\\.com)' --verbosity debug",
    "start": "npm run build && npm run start:server",
    "start:server": "browser-sync start --server --files dist --ss dist"
  },
  "watch": {
    "build": {
      "patterns": [
        "**/*"
      ],
      "extensions": "html,css",
      "ignore": [
        "node_module",
        "dist"
      ]
    }
  }
}
